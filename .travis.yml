sudo: required
services:
    - docker
before_install:
    - docker build -t gokulsubramani/docker-angular -f Dockerfile.dev .
script:
    - docker run gokulsubramani/docker-angular npm run test -- --coverage    
