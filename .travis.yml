sudo: required
services:
    - docker
before_install:
    - docker build -t gokulsubramani/docker-angular  .
before_script:
    - npm install -g @angular/cli
script:
    - npm run test-headless
    - docker run -t gokulsubramani/docker-angular
    