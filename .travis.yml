sudo: required
services:
    - docker
before_install:
    - docker build -t gokulsubramani/docker-angular -f Dockerfile .
before_script:
    - npm install -g @angular/cli
script:
    - npm run test-headless
    - docker run -t gokulsubramani/docker-angular
    